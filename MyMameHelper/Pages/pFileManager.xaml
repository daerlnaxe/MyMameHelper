<Page x:Class="MyMameHelper.Pages.pFileManager"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyMameHelper.Pages"
      xmlns:commands="clr-namespace:MyMameHelper.Commands"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" d:Background="white"
      Title="MoveATXTFile" Loaded="Page_Loaded">
    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Page.Resources>
    <Page.CommandBindings>
        <CommandBinding Command="commands:UICommands.Proceed" Executed="Proceed_Roms" />
    </Page.CommandBindings>
    <!--  -->
    <Grid>
        <DockPanel >
            <!-- Panneau de gauche -->


            <!-- Panneau du haut -->
            <StackPanel DockPanel.Dock="Top">
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="435*"/>
                        <ColumnDefinition Width="121*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label Content="Source Folder:"/>
                    <TextBox Text="{Binding Rom_Folder}" Grid.Row="0" Grid.Column="1" IsReadOnly="True"/>
                    <Label Content="Destination Folder:" Grid.Row="1" />
                    <TextBox Text="{Binding Destination_Folder}" Grid.Row="1" Grid.Column="1" IsReadOnly="True"/>
                    <Button x:Name="RB_Button" Content="Change"  VerticalAlignment="Top" Click="RB_Button_Click" Grid.Column="2" />
                    <Button x:Name="DF_Button" Content="Change" Click="DF_Button_Click" Grid.Column="2" Grid.Row="1" />
                </Grid>
            </StackPanel>


            <!-- Panneau du bas -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" FlowDirection="RightToLeft">
                <Button Name="Proceed" Content="Proceed" Command="commands:UICommands.Proceed" />
            </StackPanel>

            <StackPanel>
                <StackPanel.Resources>
                    <!-- GroupBox -->
                    <Style TargetType="GroupBox">
                        <Setter Property="Margin" Value="10,5"/>
                    </Style>
                    <!-- RadioButton -->
                    <Style TargetType="RadioButton">
                        <Setter Property="Margin" Value="5"/>
                    </Style>
                </StackPanel.Resources>
                <!-- Options -->
                <GroupBox x:Name="groupBox" Header="Options">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="19*"/>
                            <RowDefinition Height="50*"/>
                        </Grid.RowDefinitions>
                        <CheckBox x:Name="useFiles" Content="Use Files Information ???"  ToolTip="???"/>
                        <CheckBox x:Name="useUnwanted"  Content="Use Unwanted Folder (Machine method) ???" Grid.Row="1" ToolTip="???"/>
                        <!--CheckBox Content="Move files" Grid.Row="3"  ToolTip="Move files instead of copy" IsChecked="{Binding MoveFiles}"/-->

                    </Grid>
                </GroupBox>
                <!-- -->
                <GroupBox Header="File Management">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Move or Copy -->
                        <RadioButton GroupName="gpMoveOrCopy" Content="Move" IsChecked="{Binding MoveFiles}"
                                     />
                        <RadioButton GroupName="gpMoveOrCopy" Content="Copy" IsChecked="True" 
                                     Grid.Column="1"/>
                        <!-- OverWrite or Pass -->
                        <RadioButton GroupName="gpCollision" Content="OverWrite" IsChecked="{Binding OverWriteExisting}"
                                     Grid.Row="1"/>
                        <RadioButton GroupName="gpCollision" Content="Pass" IsChecked="True" 
                                     Grid.Row="1" Grid.Column="1"/>
                        <CheckBox IsChecked="{Binding OverWriteFiles, UpdateSourceTrigger=PropertyChanged}" 
                                  Content="OverWrite Files" Grid.Row="2"  ToolTip="OverWrite if file exists" Visibility="Hidden"/>
                    </Grid>
                </GroupBox>

                <StackPanel Orientation="Horizontal" Margin="10">
                    <Label Content="Arborescence type:"/>
                    <ComboBox x:Name="cbArboType" HorizontalAlignment="Left" Width="165">
                        <ComboBoxItem Content="Favorites"/>
                        <ComboBoxItem Content="Machine"/>
                        <ComboBoxItem Content="Special"/>
                    </ComboBox>
                </StackPanel>
                <!-- Panneau du milieu -->
            </StackPanel>
        </DockPanel>


    </Grid>

</Page>
