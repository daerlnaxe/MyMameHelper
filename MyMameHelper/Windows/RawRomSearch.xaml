<Window x:Class="MyMameHelper.Windows.RawRomSearch"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMameHelper.Windows"
        xmlns:commands="clr-namespace:MyMameHelper.Commands"
        mc:Ignorable="d"
        Title="Search" Height="450" Width="800" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="commands:UICommands.SearchCmd" Executed="Ex_Search"/>
        <CommandBinding Command="commands:UICommands.RemoveRom" Executed="Ex_Remove" CanExecute="Can_Remove"/>
    </Window.CommandBindings>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Paramètre de l'archive -->
        <Label Content="Archive Name"  />
        <TextBox  x:Name="tbSArchive" Text="{Binding Archive_Param, Mode=OneWayToSource}" Grid.Column="1" Width="200"/>

        <!-- Paramètre du nom du jeu -->
        <Label Content="Description" Grid.Row="1"  />
        <TextBox  x:Name="tbDescription" Text="{Binding Description, Mode=OneWayToSource}" Grid.Column="1" Width="200" Grid.Row="1"/>

        <!-- Constructeur -->
        <Label Content="Developer" Grid.Row="2"   />
        <ComboBox x:Name="cboxConstruct"  Grid.Column="1" Width="200" Grid.Row="2" ItemsSource="{Binding Developers}" SelectedValue="{Binding DevSelected}"/>
        <!-- Machine -->
        <!-- Constructeur -->
        <Label Content="Source File" Grid.Row="3"   />
        <ComboBox x:Name="cboxSourceF"  Grid.Column="1" Width="200" Grid.Row="3" ItemsSource="{Binding Source_Files}"  />
        <!-- Recherche -->
        <StackPanel Grid.Row="8" FlowDirection="RightToLeft" Orientation="Horizontal" Grid.ColumnSpan="10">
            <Button Content="Search" Command="commands:UICommands.SearchCmd"/>

        </StackPanel>
        <!-- -->
        <DataGrid x:Name="dgFound" ItemsSource="{Binding RomsFound}" Grid.Row="10" Grid.ColumnSpan="6"  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="commands:UICommands.RemoveRom" Header="Remove" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Archive Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                <DataGridTextColumn Header="Machine" Binding="{Binding Aff_Machine}" />
            </DataGrid.Columns>
    
        </DataGrid>
        <StackPanel Grid.Row="12" Orientation="Horizontal" Margin="0,5,5,5" Grid.ColumnSpan="10" FlowDirection="RightToLeft">
            <Button x:Name="btFeed" Content="Feed" Padding="5" IsDefault="True" Click="btFeed_Click"/>
            <Button Content="Cancel" Padding="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
