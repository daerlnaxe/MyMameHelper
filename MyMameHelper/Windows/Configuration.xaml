<Window x:Class="MyMameHelper.Windows.Configuration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMameHelper"
        
        
        mc:Ignorable="d"
        Title="Configuration" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Bottom">
            <Button Name="Cancel" Content="Cancel" IsCancel="True" />
            <Button Name="Save" Content="Save" Click="Save_Click" IsDefault="True"/>
        </StackPanel>
        <TabControl>
            <TabItem Header="Paths" >
                <Grid Margin="5">
                    <Grid.RowDefinitions>

                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>

                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="100*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="19*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Mame Folder:" />
                    <TextBox x:Name="tbMameFolder" Text="{Binding Mame_Folder}" Grid.Column="1" TextChanged="TbMameFolder_TextChanged"/>
                    <Button x:Name="Choose_MameFolder" Content="Choose..." Grid.Column="2" HorizontalAlignment="Left"  VerticalAlignment="Top" Width="75" Click="Choose_MameFolder_Click" />

                </Grid>
            </TabItem>
            
            
        </TabControl>
    </DockPanel>
</Window>
