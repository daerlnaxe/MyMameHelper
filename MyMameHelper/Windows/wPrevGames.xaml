<Window x:Class="MyMameHelper.Windows.wPrevGames"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMameHelper.Windows"
        mc:Ignorable="d"
        Title="wPrevList" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="441*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="311*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="53*"/>
            <RowDefinition Height="330*"/>
        </Grid.RowDefinitions>

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="123*"/>
                <ColumnDefinition Width="181*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <RadioButton GroupName="Mode" Content="Mode Archive" Checked="Mode_Changed" VerticalAlignment="Top" Margin="5" />
            <RadioButton GroupName="Mode" Content="Mode Game" Grid.Row="1" IsChecked="True" Checked="Mode_Changed" VerticalAlignment="Top" Margin="5"/>
            <Label Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" Content="{Binding Filter}" KeyUp="ListView_KeyUp" />
        </Grid>
        <!-- Liste -->

        <DataGrid x:Name="dgGames" Grid.Row="1"
                  ItemsSource="{Binding Games}" SelectedItem="{Binding Selected4List}" SelectedValuePath="ID"
                  IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Rom" Binding="{Binding Game_Name}"/>
                <DataGridTextColumn Header="Nom" Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Roms Number" Binding="{Binding Path=Roms.Count}"/>
            </DataGrid.Columns>
            <!--DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <DockPanel Margin="15,0,0,0" >
                                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                                    <TextBlock Text="{Binding Path=Parent.Game_Name}" />
                                    <TextBlock Text="{Binding Path=Parent.Archive_Name}"/>
                                </StackPanel>
                                <DataGrid ItemsSource="{Binding Path=Clones}" VerticalScrollBarVisibility="Visible">

                                </DataGrid>
                            </DockPanel>
                        </DataTemplate>
                    </-->
        </DataGrid>

        <!-- Splitter -->
        <GridSplitter x:Name="gridSplitter" Height="409" VerticalAlignment="Top" Width="5" Grid.Column="1"/>
        <!-- Presentation -->

        <Grid Grid.Column="2" Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- Nom -->
            <Label Content="Name:"/>
            <Label Content="{Binding Path=Selected4List.Game_Name}" Grid.Column="1"/>
            <!-- Parent -->
            <Label Content="Roms:" Grid.Row="1"/>
            <DataGrid Grid.Column="0" Grid.Row="2" ItemsSource="{Binding Path=Selected4List.Roms}" AutoGenerateColumns="False" IsReadOnly="True" Grid.ColumnSpan="4">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="IsParent" Binding="{Binding IsParent}"/>
                    <DataGridTextColumn Header="Archive Name" Binding="{Binding Archive_Name}"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
                    <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>





    </Grid>
</Window>
